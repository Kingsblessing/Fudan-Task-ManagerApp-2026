**后端** **AOP** **编程任务**

AOP（面向切面编程）能够将日志记录、性能统计等通用公共逻辑和核心业务代码进行解

耦，避免重复代码冗余。请自行理解 AOP 并结合课堂知识，不依赖 Spring AOP 或 AspectJ，

完成以下实验。

**（一）核心要求**

1. 实现简易 AOP 切面工具（独立于原后端）

✓ 需要为目标业务类提供正常运行模式、Debug 调试模式两种执行模式

✓ 正常运行模式：直接调用目标业务类的方法，无切面日志。

✓ Debug 调试模式：对标记了自定义注解的方法进行增强，自动打印切面日志。

2. 自定义标记注解

✓ 定义一个标记注解 @DebugLog，仅用于公共方法。在 Debug 模式下，只有被该

注解标记的方法才会被增强。

3. 被增强的方法调用时，自动打印切面日志，日志需包含：方法**调用时间**、所在**类名**、**方**

**法名**、全部**入参名称**以及对应**参数值**

4. 所有日志切面代码独立封装，不修改、不侵入任何原有业务代码。
5. 测试该工具，并将该工具应用于上次实验完成的 Spring 后端项目中，以记录 log。

**（二）验收方式**

✓ 提供完整可运行的 Java 项目，演示两种模式。✓ 在 Debug 模式下，控制台打印格式清晰的日志，不能缺少上述的任何日志内容。

✓ 正常模式下或未标记注解的方法不打印任何额外日志。

✓ 切面代码完全独立，业务类无任何耦合（业务类中不出现代理相关代码）。

完整的 AOP 切面工具源码，需包括包含：自定义切面注解、AOP 切面工具类、测试业务类、程序测试入口类。完整的后端代码，已应用你实现的 AOP 切面工具。